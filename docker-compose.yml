name: face-recognition-edge

networks:
  face-infra:
    external: true

services:
  app:
    build: .
    ports:
      - "8000:8000"
    networks:
      - face-infra     
    env_file: .env
    # devices:
    #   - /dev/video0:/dev/video0  # webcam passthrough (Linux only, not Mac)
